using TaleWorlds.CampaignSystem;
using TaleWorlds.Core;

namespace WarCouncilModern.UI
{
    public static class UIRegistry
    {
        public static void RegisterUI(IGameStarter starter)
        {
            // Placeholder: register menus/screens when UI feature is enabled
        }
    }
}